# AIUB's short-name CODE orbit files are committed byte for byte; SP3 pads its
# header records with trailing blanks, which Git's whitespace check would report.
COM*.EPH -whitespace
# The SP3 accuracy rows are fixed-width and their trailing blanks are data.
trimmed_go_static.sp3 -whitespace
